Respond with ONLY a single, FLAT JSON object using these exact top-level keys, with real values in place of the placeholders shown here:
{example}

Do NOT nest your answer under a "properties" key, and do NOT include "type", "title", or "required" keys in your answer -- those describe the schema below, they are not part of the answer. For reference, the full JSON Schema your object must satisfy is:
{schema}

Output ONLY the JSON object -- no markdown code fences, no explanation, no other text.
